import * as panel from './panel';
import * as files from './files';
import * as grid from './grid';

export function init(stores)
{
  console.log('init api', stores);
}

export {
  panel,
  grid,
  files,
}
